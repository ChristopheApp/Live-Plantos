import Typography from '@mui/material/Typography';
import { styled } from '@mui/material/styles';

const TypographyLP = styled(Typography)(() => ({
    textAlign: 'center',
    fontFamily: 'Comic Sans MS',
    fontSize: 'calc(10px + 2vmin)',
    color: "fff",
    
}));


export default TypographyLP;